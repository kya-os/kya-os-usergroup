## What

<!-- One line: which builder entry this adds or changes, or what else this PR does. -->

## Checklist for registry entries

- [ ] My entry conforms to `registry/schema/builder.schema.json` (`npm test` passes locally).
- [ ] The filename matches the `slug` field: `registry/builders/<slug>.json`.
- [ ] `homepage` (and `repo`, if given) are working `https://` links to the described project.
- [ ] The project is real and builds on KYA-OS; the description is accurate and does not misuse any trademark.
- [ ] Every commit is DCO signed off (`git commit -s`).

<!-- Not a registry entry? Delete the checklist and describe the change. -->
