{
  "name": "kya-os-usergroup",
  "version": "0.2.0",
  "private": true,
  "description": "KYA-OS community hub: PR-able registries of who builds on the protocol, what conforms to it, and the standards it carries, rendered to a static Cloudflare Pages site.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node site/build-pages.mjs",
    "test:badge": "node --test workers/badge/*.test.mjs",
    "test:conformance": "node --test scripts/__tests__/*.test.mjs",
    "test": "npm run validate && npm run build && npm run test:badge && npm run test:conformance"
  }
}
